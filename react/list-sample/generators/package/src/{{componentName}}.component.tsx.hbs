import React from 'react'
import * as S from './{{kebabCase componentName}}.styles'

export interface {{pascalCase componentName}}Props {
  testId?: string
}

export const {{pascalCase componentName}} = ({ testId = '{{kebabCase componentName}}-id', ...props }: {{pascalCase componentName}}Props) => {
  return (
    <>
      <S.{{pascalCase componentName}} data-testid={testId} {...props}>
        Body...
      </S.{{pascalCase componentName}}>
    </>
  )
}
