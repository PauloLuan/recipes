import { storiesOf } from '@storybook/react-native'
import React from 'react'
import { UserPhoto } from './UserPhoto.component'

storiesOf('Components/UserPhoto', module).add('default: ', () => <UserPhoto />)
