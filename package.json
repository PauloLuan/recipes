{
  "name": "css-learning",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npx parcel src/**/*.html --out-dir=tmp",
    "build": "npx parcel build src/**/*.html --out-dir=dist --public-url='./'",
    "format": "npx prettier-standard --format"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "babel-polyfill": "^6.26.0"
  },
  "devDependencies": {
    "autoprefixer": "^9.7.5",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "parcel-bundler": "^1.12.4",
    "postcss-modules": "^1.5.0",
    "prettier-standard": "^16.3.0"
  }
}
